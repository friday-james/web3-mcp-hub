# All API keys are optional - the server works without any keys using free APIs

# Optional API Keys (for higher rate limits)
COINGECKO_API_KEY=

# Custom RPC URLs (optional - defaults to public RPCs)
RPC_ETHEREUM=
RPC_BASE=
RPC_ARBITRUM=
RPC_POLYGON=
RPC_OPTIMISM=
RPC_SOLANA=
RPC_OSMOSIS=
RPC_COSMOSHUB=

# Server Configuration
DEFAULT_SLIPPAGE_BPS=50
